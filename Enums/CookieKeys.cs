namespace rstracker.Enums
{
    public enum CookieKeys
    {

        /* This cookie is used to store the consent of the user to the use of cookies. */

        COOKIE_CONSENT,

        /* Recent searches will store a list of the last 5 players that the user has searched for. */

        RECENT_SEARCHES

    }
}
